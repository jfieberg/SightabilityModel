#'    
#' @param stratum.data A data frame containing for each combination of stratum and domain, the stratum id (see stratum.var), 
#' the domain id (see domain.var),
#' the total number of blocks in the stratum (see stratum.total.blocks.var) and 
#' the total area of the stratum (see stratum.total.area.var)
#' @template stratum.var 
#' @param stratum.total.blocks.var Name of the variable in the stratum.data data frame that contains the total number of
#'    blocks in the stratum.
#' @param stratum.total.area.var  Name of the variable in the stratum.data data.frame that contains the total stratum area.
#'
