#' @param selected.unit.data A data frame containing information on the selected survey units. Required variables
#' are the stratum (see stratum.var), domain (see domain.var), block.id (see block.id.var), and the
#'    area of the block (see block.area.var). 
#' @template block.id.var
#' @template block.area.var
